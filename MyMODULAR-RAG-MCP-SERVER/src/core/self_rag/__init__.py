"""Self-RAG online inference components.

This package holds the reflection control plane (response parsing, retrieval
gating, critics, generation, and orchestration). It depends on the traditional
RAG core, never the other way around.
"""
